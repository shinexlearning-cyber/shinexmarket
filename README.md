# SHINEX Marketplace Frontend

Existing SHINEX single-file React frontend packaged as a Vite + Tailwind project.

API: https://shinex-marketplace.onrender.com/api

Build command: `npm run build`
Start/deploy as a static Vite site on Render using the generated `dist` folder.
