import {useTheme} from '../hooks/useTheme';import {tokenStore} from '../api/client';import {Link} from 'react-router-dom';import {State} from '../components/UI';
export default function Settings(){const [theme,setTheme]=useTheme();return <section className="container page narrow"><div className="page-head"><span className="eyebrow">Preferences</span><h1>Settings</h1></div><div className="panel setting-row"><div><b>Theme</b><span>Choose how SHINEX looks. Only this preference is stored locally.</span></div><select value={theme} onChange={e=>setTheme(e.target.value)}><option value="system">System</option><option value="light">Light</option><option value="dark">Dark</option></select></div><div className="panel setting-links"><Link to="/profile">Profile & shop</Link><Link to="/support">Support</Link><Link to="/reports">My reports</Link>{tokenStore.get()&&<Link to="/favorites">Favorites</Link>}</div><State title="No other local data is stored" body="Marketplace data comes from the SHINEX backend rather than localStorage."/></section>}
